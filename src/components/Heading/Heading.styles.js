import styled from "styled-components";
import { PRIMARY, WHITE } from "@/styles/globalStyle";

export const HeadingStyled = styled.h2`
  color: ${WHITE};
  margin-bottom: 7.5rem;
  span {
    color: ${PRIMARY};
  }
`;
