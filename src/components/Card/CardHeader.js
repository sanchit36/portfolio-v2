import React from "react";
import { CardHeaderContainer } from "./Card.styles";
const CardHeader = ({ children }) => {
  return <CardHeaderContainer>{children}</CardHeaderContainer>;
};

export default CardHeader;
