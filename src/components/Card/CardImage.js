import React from "react";
import { CardImageContainer } from "./Card.styles";

const CardImage = ({ children }) => {
  return <CardImageContainer>{children}</CardImageContainer>;
};

export default CardImage;
